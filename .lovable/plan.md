

# Update Favicon

Replace the current favicon with the uploaded Tarun Chess Academy logo.

## Changes

1. **Copy uploaded file** to `public/favicon.ico`
2. **Update `index.html`** — ensure the `<link rel="icon">` tag points to `/favicon.ico`

No other files need changes. This works on both desktop and mobile browsers.

